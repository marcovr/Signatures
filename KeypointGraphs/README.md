# KeypointGraphs

Extracts graphs from images.

# Setup instructions

Just execute:

```./setup```

Or, without using a virtual environment:

```pip install -r requirements.txt```

Then, run with:

```python3 run.py```
